<!DOCTYPE html>
<html data-theme="light">
  <head>
    <title><%= content_for(:title) || "運転代行管理システム" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="運転代行管理システム">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Tailwind CSS %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-base-200">
    <!-- サイドバー（PC/タブレット） -->
    <% if driver_signed_in? %>
      <%= render "shared/sidebar" %>
    <% end %>

    <!-- メインコンテンツエリア -->
    <div class="<%= driver_signed_in? ? 'md:pl-64 lg:pl-72' : '' %>">
      <div id="flash-messages" class="toast toast-top toast-end z-50">
        <% if notice %>
          <div class="alert alert-success shadow-lg" data-controller="flash" data-flash-duration-value="3000">
            <span><%= notice %></span>
          </div>
        <% end %>

        <% if alert %>
          <div class="alert alert-error shadow-lg" data-controller="flash" data-flash-duration-value="3000">
            <span><%= alert %></span>
          </div>
        <% end %>
      </div>

      <%= yield %>
    </div>
  </body>
</html>
